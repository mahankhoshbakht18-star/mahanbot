name: Quality checks

on:
  pull_request:
  push:
    branches:
      - main
      - "refactor/**"

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          show-progress: false

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Apply phase 1-4 migration in validation workspace
        id: migration
        if: ${{ hashFiles('scripts/run_phase1_4_refactor.py') != '' }}
        shell: bash
        run: |
          set +e
          python scripts/run_phase1_4_refactor.py > migration.log 2>&1
          status=$?
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload migration diagnostics
        if: ${{ steps.migration.outputs.status != '0' }}
        uses: actions/upload-artifact@v4
        with:
          name: migration-diagnostics
          path: migration.log
          if-no-files-found: error
          retention-days: 7

      - name: Stop on migration failure
        if: ${{ steps.migration.outputs.status != '0' }}
        shell: bash
        run: |
          cat migration.log
          exit 1

      - name: Install lightweight validation dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install fastapi

      - name: Run quality gate
        id: quality_gate
        shell: bash
        run: |
          set +e
          python scripts/quality_check.py 2>&1 | tee quality.log
          status=${PIPESTATUS[0]}
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload quality diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: quality-diagnostics
          path: quality.log
          if-no-files-found: error
          retention-days: 7

      - name: Stop on quality failure
        if: ${{ steps.quality_gate.outputs.status != '0' }}
        shell: bash
        run: |
          cat quality.log
          exit 1

      - name: Build clean source archive
        shell: bash
        run: |
          mkdir -p dist
          zip -r dist/mahanbot-complete-source.zip . \
            -x ".git/*" \
               "dist/*" \
               "*.db" \
               "*.db-wal" \
               "*.db-shm" \
               ".env" \
               ".env.*" \
               "migration.log" \
               "quality.log" \
               "chrome_profiles/*" \
               "screenshots/*" \
               "archives/*" \
               "__pycache__/*" \
               "*/__pycache__/*"

      - name: Upload clean source archive
        uses: actions/upload-artifact@v4
        with:
          name: mahanbot-complete-source
          path: dist/mahanbot-complete-source.zip
          if-no-files-found: error
          retention-days: 30
