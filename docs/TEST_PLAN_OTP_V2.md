# برنامه تست OTP v2

Backend: fail-closed، pairing expiry/single-use/attempt، token hash/revoke، session mismatch،
timestamp، duplicate/idempotency، delivery lease/consume، concurrency، body/CORS/log.
Android: normalization، ambiguity، permissions دو flavor، consent/manual، queue/reboot/retry.
MahanBot: session-before-wait، job isolation، timeout/cancel/consume، final_submit=false و CAPTCHA hash.
