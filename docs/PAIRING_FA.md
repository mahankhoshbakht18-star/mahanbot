# Pairing

Dashboard با bot authentication کد ۶ یا ۸ رقمی پنج‌دقیقه‌ای می‌سازد. کد یک‌بارمصرف و دارای
حداکثر پنج تلاش است. اپ code یا QR را claim می‌کند؛ server شناسه عمومی و token خام یک‌باره
می‌دهد. server فقط hash token را نگه می‌دارد و اپ آن را با Android Keystore محافظت می‌کند.
