# نگهداری داده

OTP تا consume یا expiry نگهداری و سپس ciphertext پاک می‌شود. Pairing code پنج دقیقه، delivery
lease سی ثانیه و session حداکثر ده دقیقه است. Audit فقط actor/result و metadata ماسک‌شده دارد.
backup دیتابیس باید در rest رمز و دسترسی آن محدود باشد.
